# Install dependencies. Ranges mirror pyproject.toml.
# For reproducible installs, use: pip install -e ".[dev,spot,perception]"
# or generate a lockfile with pip-compile / uv lock.

# Core
pydantic>=2.7,<3
PyYAML>=6,<7

# Dev
pytest>=8,<9
ruff>=0.4.0,<1

# REPL
cmd2>=3.4,<4

# Strands (agent orchestration)
strands-agents>=1.34,<2
strands-agents-tools>=0.3,<1

# Bedrock (model serving)
boto3>=1.26,<2
botocore>=1.29,<2

# Spot SDK
bosdyn-client>=5.1,<5.2
bosdyn-mission>=5.1,<5.2

# Perception (point cloud + image processing)
opencv-python>=4.8,<5
numpy>=1.24,<3
